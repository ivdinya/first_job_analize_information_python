#создадим функцию для проверки с исключениями
# принимаем две переменных
def iskl(first, second):
    #переводим число в строку, создав отдельную переменную
    first_stroka = str(first)
    #здесь, мы смотрим длину строки,если она не равно 10,то мы возвращаем строку с ошибкой 
    if len(first_stroka) != 10:
        return "Твое число не десятизначное ("
    #здесь мы перебираем каждый симфол строки и проверям если оно не число, то выводим ошибку
    for i in first_stroka:
        if not i.isdigit():
            return "Не подходящий символ ("
    #возвращаем функцию бинарного алгоритма евклида
    return binar(int(first), int(second))
